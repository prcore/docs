---
properties:
  - name: file
    type: string($binary)
    description: The file of the event log
    required: true
  - name: seperator
    type: str
    description: The seperator of the CSV event log
    required: false
    defaultValue: ','
  - name: test
    type: string($binary)
    description: The test file of the event log
    required: false
